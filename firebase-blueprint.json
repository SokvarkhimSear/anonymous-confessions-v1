{
  "entities": {
    "Room": {
      "title": "Room",
      "description": "A private group for anonymous confessions.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the room." },
        "createdBy": { "type": "string", "description": "The UID of the user who created the room." },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the room was created." }
      },
      "required": ["name", "createdBy", "createdAt"]
    },
    "Confession": {
      "title": "Confession",
      "description": "An anonymous confession posted in a room.",
      "type": "object",
      "properties": {
        "text": { "type": "string", "description": "The content of the confession." },
        "animalAlias": { "type": "string", "description": "The random animal name assigned to the author for this post." },
        "authorUid": { "type": "string", "description": "The UID of the author (for security rules)." },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the confession was posted." }
      },
      "required": ["text", "animalAlias", "authorUid", "createdAt"]
    }
  },
  "firestore": {
    "/rooms/{roomId}": {
      "schema": "Room",
      "description": "A collection of private confession rooms."
    },
    "/rooms/{roomId}/confessions/{confessionId}": {
      "schema": "Confession",
      "description": "A sub-collection of anonymous confessions within a room."
    }
  }
}
